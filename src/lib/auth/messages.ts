/** Shown when Supabase client is missing in the browser (misconfigured env). */
export const AUTH_NOT_CONFIGURED_MESSAGE =
  "Authentication is not configured. Check environment variables." as const;
